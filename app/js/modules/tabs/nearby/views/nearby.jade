ion-view(hide-nav-bar="true")

  ion-header-bar.bar-positive.item-input-inset
      button.button.button-clear(ng-click="openModal()") Filter&nbsp&nbsp
      label.item-input-wrapper
        i.icon.ion-ios7-search.placeholder-icon
        form(ng-submit="newSearch(search)")
          input(type="search" placeholder="Search" ng-model="search")
      a.button.button-clear(href="#/tab/nearby/map") Map

  ion-content.bg-grey
    div.list
      a.item(ng-repeat="business in businesses | filter:search" href="#/tab/nearby/menu/{{business.factual_id}}")
        //- img(src='{{business.image_url}}')
        h2 {{business.name}}
        h4 {{business.address + " " + business.locality }}
        p {{business.tel}}
