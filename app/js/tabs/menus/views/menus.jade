ion-view(hide-nav-bar="true" id="places")
  div(class="bar bar-stable bar-header item-input-inset")
    button.button.button-clear.button-assertive(ng-click="openModal()") Filter&nbsp&nbsp
    label(class="item-input-wrapper")
      i(class="icon ion-ios7-search placeholder-icon")

      input(type="text" g-places-autocomplete class="form-control" ng-model="place" placeholder="Search")
      //- input(id="searchQuery" class="google-place-search" type="search" ng-model="location" placeholder="Enter an address, place or ZIP code")
      //- ng-places(placeholder="Enter an address, Apt# and ZIP" ng-model="location")
      //- input(type="text" id="Autocomplete" class="form-control" ng-autocomplete ng-model="result1")
      //<div>selection is: {{chosenPlace}}</div>

      //- input(ng-model="chosenPlace" googleplace)
      //- form(class="form")


    a.button.button-clear.button-assertive(href="#/tab/menus/map") Map



  ion-content(class="has-header has-header")
    //- div result: {{result1}}
    div.list

      a.item(ng-repeat="location in vm.locations | orderBy: 'dist'" type="item-text-wrap" href="#/tab/menus/menu/{{location._id}}")
        h2 {{location.name}}
        h4.text-left {{location.address + " - " + location.city}}
        span.text-right
          i.icon.ion-navigate(style="color:grey;") &nbsp
          span {{location.dist}} mi



    //- ion-header-bar.bar-positive.item-input-inset
    //-   button.button.button-clear(ng-click="openModal()") Filter&nbsp&nbsp
    //-
    //-   label.item-input-wrapper
    //-     i.icon.ion-ios7-search.placeholder-icon
    //-     form(ng-submit="newSearch(menusFilter)")
    //-       input(type="search" placeholder="Search" ng-model="menusFilter")
    //-
    //-   a.button.button-clear(href="#/tab/menus/map") Map
    //-
    //- ion-content
    //-   div.list
    //-     a.item(ng-repeat="business in businesses | filter:menusFilter" href="#/tab/menus/menu/{{business.id}}")
    //-       //- img(src='{{business.image_url}}')
    //-       h2 {{business.name}}
    //-       h4(ng-if="business.address") {{business.address + " " + business.locality }}
    //-       h4(ng-if="business.street_address") {{business.street_address + " " + business.locality }}
    //-       p(ng-if="business.phone"){{business.phone}}
    //-       p(ng-if="business.tel") {{business.tel}}
